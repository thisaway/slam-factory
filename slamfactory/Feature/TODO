### Detector
- FAST
  * 效率优化。在对关键点进行排序时，不用把完全降序排列，只需要找出前n个score较大的关键点即可；

  * 空间优化。一些无效的关键点仍然占有一定空间，如果无效的关键点不用bad位标识，而是用score=-1标识， \
    这样就可以通过排序进行将这些点进行剔除了。但是仍然存在的问题是，在网格化的FAST中，即使将这些点排序后面，
    仍然无法减小空间，只有将现在的1维的vector拓展为2维才可以，但2维也会导致nms需要的索引矩阵发生改变；

  * 类和结构体内成员变量的重排，保证内存对齐；